type: object
required:
  - type
  - addressId
properties:
  type:
    type: string
    enum:
      - "verified_blockchain"
  addressId:
    description: "The ID of the verified blockchain recipient address."
    type: string
    format: uuid

