GenerateAddressRequest:
  type: object
  required:
    - idempotencyKey
    - currency
    - chain
  properties:
    idempotencyKey:
      $ref: "./IdempotencyKey.yml"
    currency:
      $ref: "./Currency.yml"
    chain:
      $ref: "./Chain.yml"
